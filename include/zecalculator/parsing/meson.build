if not meson.is_subproject()
  install_headers(
    files(
      'parser.h',
      'types.h',
    ),
    subdir: 'zecalculator',
    preserve_path: true
  )
endif

subdir('data_structures')
subdir('decl')
subdir('impl')
